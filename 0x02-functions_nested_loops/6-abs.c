#include "main.h"

/**
 *_abs - function to print absolute value of a number
 *@r: interger whose absolute value will be returned
 *Return: Always 0 (Success)
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
