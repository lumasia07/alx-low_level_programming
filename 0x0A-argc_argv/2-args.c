#include "main.h"
#include <stdio.h>

/**
 * main - prints all argsit receives
 * @argc: argument counter
 * @argv: argument vector
 * Return: Always 0 (Success)
 */
int main(int argc, char **argv)
{
	(void)argv;

	printf("%d\n", argc);
	printf("\n");

	return (0);
}
